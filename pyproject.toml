[project]
name = "uipath-agenthack"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40.0",
    "requests>=2.32.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
